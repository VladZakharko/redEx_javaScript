// let str = `Creator It Academy`;

// console.log(str.match(/creator/gi));

// let matches = `JavaScript`.match(/HTMl/) || [];

// if(!matches.length) {
//     console.log(`Співпадіння немає`)
// }

console.log(`We will, we will`.replace(/we/i, `I`));